1. Totals doesnt float alongside to the right as you scroll. I would prefer this.
2. Add the ability to save multiple plans, along with the ability to Name them.
3. Add the ability to share a plan with a url (Maybe it counts as an import when clicked.)
4.  Add the ability to rearrange rows in the sources table.
5. Add a filter to the Capped Resources Leftovers section that ignores missions. Missions will never have "leftovers"
6. Add a feature that shows the ship and stats when hovering over. Almost all WoWs pages on their site have this feature.
I'd love to have something like this that shows the stats, rarity, name, ship image, etc. Similar to how my WoWs Collection Calculator works,
we could pull from the Wargaming API to get all that info nightly, then just reference it on this app.

7. Trim a little bit off the column width. Adding the grabber cells made the rows really wide. We can tighten these up.
8. What is this Toast thing?